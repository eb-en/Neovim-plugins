return {
  "mg979/vim-visual-multi",
  -- Load immediately to ensure keybinds work on startup
  lazy = false,
  init = function()
    -- You can add custom VM configurations here if needed
    -- Example: vim.g.VM_leader = "\\"
  end,
}
